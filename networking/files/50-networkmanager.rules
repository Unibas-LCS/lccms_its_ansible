// /etc/polkit-1/rules.d/50-networkmanager.rules
polkit.addRule(function(action, subject) {
    if (action.id.startsWith("org.freedesktop.NetworkManager.") &&
        subject.isInGroup("uni-all")) {
        return polkit.Result.YES;
    }
});